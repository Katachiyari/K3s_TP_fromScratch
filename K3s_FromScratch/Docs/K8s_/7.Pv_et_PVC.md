📘 Cours Kubernetes (Upstream) — Chapitre 7

💾 Stockage persistant : Volumes, PV et PVC


---

🎯 Objectifs pédagogiques

À la fin de ce chapitre tu sauras :

Pourquoi les Pods ont un stockage éphémère

Ce qu’est un Volume Kubernetes

Comprendre les rôles de PersistentVolume (PV) et PersistentVolumeClaim (PVC)

Utiliser un stockage persistant dans une application



---

🧠 1. Problème : le stockage des Pods est éphémère

Quand un Pod est recréé :

Son système de fichiers est perdu

Les données locales disparaissent


👉 Problème pour :

Bases de données

CMS (WordPress, GLPI…)

Applications avec fichiers utilisateurs



---

🧩 2. Les Volumes

Un Volume est un espace de stockage monté dans un Pod.

Il peut être :

Type	Usage

emptyDir	Temporaire
configMap	Fichiers de config
secret	Données sensibles
persistent	Données durables


Documentation officielle :
https://kubernetes.io/docs/concepts/storage/volumes/


---

💽 3. Le stockage persistant Kubernetes

Kubernetes sépare :

Élément	Rôle

PV (PersistentVolume)	Ressource de stockage réelle
PVC (PersistentVolumeClaim)	Demande de stockage par un Pod


C’est un modèle abstraction fournisseur / consommateur.


---

🧱 4. PersistentVolume (PV)

Le PV représente un stockage disponible dans le cluster :

apiVersion: v1
kind: PersistentVolume
metadata:
  name: demo-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/demo


---

📥 5. PersistentVolumeClaim (PVC)

Le PVC est la demande faite par une application :

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: demo-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

Kubernetes associe automatiquement PVC ↔ PV compatible.


---

🔌 6. Utiliser le stockage dans un Pod

apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-app
  template:
    metadata:
      labels:
        app: demo-app
    spec:
      containers:
      - name: app
        image: nginx
        volumeMounts:
        - name: storage
          mountPath: /usr/share/nginx/html
      volumes:
      - name: storage
        persistentVolumeClaim:
          claimName: demo-pvc


---

🔄 7. Cycle de vie

1. Création du PV (admin)


2. Création du PVC (application)


3. Binding automatique


4. Pod utilise le volume


5. Pod supprimé → données conservées




---

🧠 8. Modes d’accès

Mode	Signification

ReadWriteOnce	Monté en lecture/écriture par un seul nœud
ReadOnlyMany	Lecture seule par plusieurs nœuds
ReadWriteMany	Lecture/écriture par plusieurs nœuds



---

⚙️ 9. StorageClass (provisionnement dynamique)

Permet de créer automatiquement des volumes via un backend (cloud, SAN, NFS…)

storageClassName: standard

Sans StorageClass → provisioning manuel requis.

Documentation officielle :
https://kubernetes.io/docs/concepts/storage/storage-classes/


---

🧪 10. Mise en application complète

kubectl apply -f pv.yaml
kubectl apply -f pvc.yaml
kubectl apply -f deployment.yaml

Vérifier :

kubectl get pv
kubectl get pvc
kubectl get pods


---

📌 Résumé du chapitre

Élément	Rôle

Volume	Stockage monté dans un Pod
PV	Ressource de stockage
PVC	Demande de stockage
StorageClass	Provisionnement dynamique



---

🎓 À retenir

> Les Pods sont éphémères,
les volumes persistants gardent les données.




---

🔜 Chapitre suivant

📊 Supervision et métriques (Metrics Server, HPA)
